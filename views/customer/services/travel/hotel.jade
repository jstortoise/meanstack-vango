extends ../../layout
block pageStyle
  link(rel='stylesheet', href='/stylesheets/customer/services/travel.css')
block pageContent
  a.back-btn(href='/services/travel') BACK
  section.section2
    .blank
    .inner
      .header
        .header-back
          h3
            | pago de
            br
            | hotel
          .comment
            p En caso de robo o avería de tu vehículo ¿Necesitas hospedarte en el lugar? Vango! Hace la reservación y cubre el costo, en un Hotel cómodo y confortable, por ello dicen que Vango! Es más que un seguro.
      .mobile-comment
        p En caso de robo o avería de tu vehículo ¿Necesitas hospedarte en el lugar? Vango! Hace la reservación y cubre el costo, en un Hotel cómodo y confortable, por ello dicen que Vango! Es más que un seguro.
    .bottom
      a.main-button(href='#')
        ¡Obtén cobertura ya!

block pageScript
  script.
    $('.navbar').addClass('top');
    $('.navbar .btn-consult').hide();
    $('.navbar .btn-social').show();
    $( window ).scroll(function() {
      var scroll = $(window).scrollTop();
      if(scroll < 10){
        $('.navbar .btn-consult').hide();
        $('.navbar .btn-social').show();
      }else{
        $('.navbar .btn-consult').show();
        $('.navbar .btn-social').hide();

      }
    });

    $('.nav-services').addClass('active');
    $('.logo-large').attr('src', '/images/customer/services/logo_lg.png');
    $('.logo-medium').attr('src', '/images/customer/services/logo_md.png');
    $('.logo-small').attr('src', '/images/customer/services/logo_sm.png');

    $('#blogmenu').attr('href', 'https://asistenciahoteles.segurosvango.com');
